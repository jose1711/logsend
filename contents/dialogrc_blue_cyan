#
# Run-time configuration file for dialog
#
# Automatically generated by "dialog --create-rc <file>"
#
#
# Types of values:
#
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?)
#


# Shadow dialog boxes? This also turns on color.
use_shadow = ON

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (CYAN,BLUE,OFF)

# Shadow color
shadow_color = (BLACK,BLACK,ON)

# Dialog box color
dialog_color = (WHITE,CYAN,ON)

# Dialog box title color
title_color = (BLUE,CYAN,OFF)

# Dialog box border color
border_color = (WHITE,CYAN,ON)

# Active button color
button_active_color = (BLUE,BLUE,OFF)

# Inactive button color
button_inactive_color = (CYAN,CYAN,OFF)

# Active button key color
button_key_active_color = (BLUE,BLUE,ON)

# Inactive button key color
button_key_inactive_color = (BLACK,CYAN,OFF)

# Active button label color
button_label_active_color = (CYAN,BLUE,ON)

# Inactive button label color
button_label_inactive_color = (WHITE,CYAN,ON)

# Input box color
inputbox_color = (CYAN,BLUE,ON)

# Input box border color
inputbox_border_color = (BLUE,BLUE,ON)

# Search box color
searchbox_color = (BLACK,CYAN,OFF)

# Search box title color
searchbox_title_color = (BLUE,CYAN,OFF)

# Search box border color
searchbox_border_color = (CYAN,CYAN,ON)

# File position indicator color
position_indicator_color = (CYAN,BLUE,ON)

# Menu box color
menubox_color = (BLUE,BLUE,ON)

# Menu box border color
menubox_border_color = (BLUE,BLUE,ON)

# Item color
item_color = (CYAN,BLUE,OFF)

# Selected item color
item_selected_color = (CYAN,BLUE,ON)

# Tag color
tag_color = (CYAN,BLUE,OFF)

# Selected tag color
tag_selected_color = (CYAN,BLUE,ON)

# Tag key color
tag_key_color = (CYAN,BLUE,OFF)

# Selected tag key color
tag_key_selected_color = (CYAN,BLUE,ON)

# Check box color
check_color = (CYAN,BLUE,OFF)

# Selected check box color
check_selected_color = (CYAN,BLUE,ON)

# Up arrow color
uarrow_color = (BLUE,CYAN,ON)

# Down arrow color
darrow_color = (BLUE,CYAN,ON)

# Item help-text color
itemhelp_color = (CYAN,BLACK,OFF)

# Active form text color
# form_active_text_color = (CYAN,BLACK,ON)

# Form text color
# form_text_color = (CYAN,CYAN,ON)
