#
# Run-time configuration file for dialog
#
# Automatically generated by "dialog --create-rc <file>"
#
#
# Types of values:
#
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?)
#


# Shadow dialog boxes? This also turns on color.
use_shadow = OFF

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (WHITE,BLUE,OFF)

# Shadow color
shadow_color = (BLACK,BLACK,ON)

# Dialog box color
dialog_color = (BLUE,WHITE,OFF)

# Dialog box title color
title_color = (WHITE,WHITE,ON)

# Dialog box border color
border_color = (BLUE,WHITE,ON)

# Active button color
button_active_color = (BLUE,BLUE,OFF)

# Inactive button color
button_inactive_color = (WHITE,WHITE,OFF)

# Active button key color
button_key_active_color = (BLUE,BLUE,ON)

# Inactive button key color
button_key_inactive_color = (WHITE,WHITE,ON)

# Active button label color
button_label_active_color = (WHITE,BLUE,ON)

# Inactive button label color
button_label_inactive_color = (BLACK,WHITE,OFF)

# Input box color
inputbox_color = (BLUE,WHITE,OFF)

# Input box border color
inputbox_border_color = (BLUE,WHITE,ON)

# Search box color
searchbox_color = (BLACK,WHITE,OFF)

# Search box title color
searchbox_title_color = (BLUE,WHITE,ON)

# Search box border color
searchbox_border_color = (WHITE,WHITE,ON)

# File position indicator color
position_indicator_color = (BLUE,WHITE,ON)

# Menu box color
menubox_color = (WHITE,WHITE,ON)

# Menu box border color
menubox_border_color = (WHITE,WHITE,ON)

# Item color
item_color = (BLACK,WHITE,OFF)

# Selected item color
item_selected_color = (WHITE,BLUE,ON)

# Tag color
tag_color = (BLACK,WHITE,OFF)

# Selected tag color
tag_selected_color = (BLUE,WHITE,OFF)

# Tag key color
tag_key_color = (BLUE,WHITE,ON)

# Selected tag key color
tag_key_selected_color = (BLUE,WHITE,OFF)

# Check box color
check_color = (BLUE,WHITE,OFF)

# Selected check box color
check_selected_color = (WHITE,BLACK,ON)

# Up arrow color
uarrow_color = (WHITE,WHITE,ON)

# Down arrow color
darrow_color = (WHITE,WHITE,ON)

# Item help-text color
itemhelp_color = (WHITE,BLACK,OFF)

# Active form text color
# form_active_text_color = (WHITE,BLACK,ON)

# Form text color
# form_text_color = (WHITE,WHITE,ON)
